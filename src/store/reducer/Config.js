
const defaultState = {
    status:[
        {label:'禁用',value:0},
        {label:'启用',value:1}
    ]
  }
  
 const ConfigReducer = function(state = defaultState, actions){
    return state
  }

  export default ConfigReducer